/*
If test 3 (from semantic-1/if3): arithmetic operations in condition
*/

fn main() {
    let n: i32 = 8;
    if (n % 2 == 0) {
        let even: bool = true;
        printlnInt(even as i32);
    } else {
        let even: bool = false;
        printlnInt(even as i32);
    }
    exit(0);
}
