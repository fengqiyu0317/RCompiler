/*
If test 6 (from semantic-1/if6): nested if in while loop
*/

fn main() {
    let mut counter: i32 = 0;
    while (counter < 3) {
        if (counter == 1) {
            counter += 1;
        } else {
            counter += 1;
        }
    }
    printlnInt(counter);
    exit(0);
}
