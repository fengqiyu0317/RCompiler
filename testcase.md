### 数据测试
#### 步骤一：编译词法分析程序
#### 步骤二：生成输出结果
- RCompiler-Testcases文件夹目录下有两个文件夹semantic-1和semantic-2，里面有很多文件夹，每个文件夹有一个以rx为后缀名的文件，请将每个这样的文件作为输入运行目录下的词法分析程序，并对每个输入输出结果，将结果保存在.txt文件中，并分类存至result为名字的文件夹中。
- 例如：在semantic-1中有一个叫array1的文件夹，其中有一个叫`array1.rx`的文件，将其作为输入运行词法分析程序：
  - `java Main < input > output`
- 可以得到这个文件中rust程序的词法分析结果。将该结果保存至`.\result\semantic-1\array1.txt`中。
- 注意：因为输入数据是不同的，所以应当有不同的输出结果。